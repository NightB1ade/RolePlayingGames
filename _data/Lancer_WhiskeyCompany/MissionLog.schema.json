{
	"type": "array"
	, "title": "Log Entries"
	, "format": "table"
	, "uniqueItems": true
	, "items": {
		"type": "object"
		, "title": "Log Entry"
		, "required": [
			"startDate"
			, "name"
			, "link"
		]
		, "properties": {
			"startDate": {
				"type": "string"
				, "title": "Start Date"
				, "format": "date"
			}
			, "endDate": {
				"type": "string"
				, "title": "End Date"
				, "format": "date"
			}
			, "name": {
				"type": "string"
				, "title": "Name"
			}
			, "link": {
				"type": "string"
				, "title": "Link"
			}
		}
	}
}
